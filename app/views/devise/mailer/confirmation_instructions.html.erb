<p><%= t('.greeting', default: "Welcome!") %></p>

<p><%= t('.instruction', default: "You can confirm your account email through the link below:") %></p>

<p><%= link_to t('.action', default: "Confirm my account"), confirmation_url(@resource, confirmation_token: @token) %></p>

<p><%= t('.footer', default: "Footer!") %></p>
